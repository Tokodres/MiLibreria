@model MiLibreria.ViewModels.CompraDetalleViewModel

<div class="container">
    <h2 class="mb-4">Registrar Nueva Compra</h2>

    <form asp-action="Create" asp-controller="Compras" method="post" id="compraForm">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

        <!-- Sección de Datos Principales -->
        <div class="card mb-4">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0">Datos de la Compra</h5>
            </div>
            <div class="card-body">
                <div class="row">


                    <div class="col-md-4 mb-3">
                        <label asp-for="NumeroFactura" class="form-label required">Número de Factura</label>
                        <input asp-for="NumeroFactura" class="form-control" required />
                        <span asp-validation-for="NumeroFactura" class="text-danger"></span>
                    </div>

                    <div class="col-md-4 mb-3">
                        <label asp-for="FechaCompra" class="form-label required">Fecha</label>
                        <input asp-for="FechaCompra" class="form-control" type="date" required
                               value="@DateTime.Now.ToString("yyyy-MM-dd")" />
                        <span asp-validation-for="FechaCompra" class="text-danger"></span>
                    </div>

                  
                </div>

                <div class="row">
                    <div class="col-md-12 mb-3">
                        <label asp-for="ProveedorId" class="form-label required">Proveedor</label>
                        <select asp-for="ProveedorId"
                                asp-items="@(new SelectList(Model.Proveedores, "ProveedorId", "PersonaContacto"))"
                                class="form-select"
                                required>
                            <option value="">Seleccione un proveedor</option>
                        </select>
                        <span asp-validation-for="ProveedorId" class="text-danger"></span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Sección de Detalles -->
        <div class="card mb-4">
            <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                <h5 class="mb-0">Detalle de Productos</h5>
            </div>
            <div class="card-body">
                <div class="row mb-3">
                    <div class="col-md-4">
                        <label for="LibroId" class="form-label">Libro</label>
                        <select id="LibroId" class="form-select">
                            <option value="">Seleccione un libro</option>
                            @foreach (var libro in Model.Libros)
                            {
                                <option value="@libro.LibroId"
                                        data-precio="@libro.PrecioCompra"
                                        data-stock="@libro.Stock">
                                    @libro.Titulo 
                                </option>
                            }
                         
                        </select>
                    </div>

                    <div class="col-md-2">
                        <label for="Cantidad" class="form-label">Cantidad</label>
                        <input type="number" id="Cantidad" class="form-control" min="1" />
                        <small class="text-muted" id="stockDisponible"></small>
                    </div>

                    <div class="col-md-3">
                        <label for="PrecioUnitario" class="form-label">Precio Unitario</label>
                        <input type="number" id="PrecioUnitario" class="form-control" min="0.01" step="0.01" />
                    </div>

                    <div class="col-md-3">
                        <label class="form-label">&nbsp;</label>
                        <button type="button" class="btn btn-success w-100" id="btnAdicionar">
                            <i class="bi bi-plus-circle"></i> Agregar Producto
                        </button>
                    </div>
                </div>

                <div class="table-responsive">
                    <table class="table table-striped table-bordered" id="tablaDetalles">
                        <thead class="table-light">
                            <tr>
                                <th>Libro</th>
                                <th class="text-center">Cantidad</th>
                                <th class="text-end">Precio Unit.</th>
                                <th class="text-end">Subtotal</th>
                                <th class="text-center">Acciones</th>
                            </tr>
                        </thead>
                        <tbody id="detalleTabla">
                        </tbody>
                        <tfoot>
                            <tr>
                                <td colspan="3" class="text-end"><strong>Total:</strong></td>
                                <td class="text-end">
                                    <strong>$ <span id="totalCompra">0.00</span></strong>
                                </td>
                                <td></td>
                            </tr>
                        </tfoot>
                    </table>
                </div>

                <div id="detallesCompraContainer"></div>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                <button type="submit" class="btn btn-primary" id="btnGuardar">
                    <i class="bi bi-save"></i> Guardar Compra
                </button>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> Volver
                </a>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const detallesCompra = [];
            let contadorDetalles = 0;

            // Elementos del DOM
            const libroSelect = document.getElementById('LibroId');
            const cantidadInput = document.getElementById('Cantidad');
            const precioInput = document.getElementById('PrecioUnitario');
            const stockSpan = document.getElementById('stockDisponible');
            const btnAdicionar = document.getElementById('btnAdicionar');
            const detalleTabla = document.getElementById('detalleTabla');
            const totalCompraSpan = document.getElementById('totalCompra');
            const detallesContainer = document.getElementById('detallesCompraContainer');
            const form = document.getElementById('compraForm');

            // Event Listeners
            libroSelect.addEventListener('change', actualizarPrecioYStock);
            btnAdicionar.addEventListener('click', agregarDetalle);
            form.addEventListener('submit', validarFormulario);

            function actualizarPrecioYStock() {
                const option = libroSelect.selectedOptions[0];
                if (option.value) {
                    const precio = option.dataset.precio;
                    const stock = option.dataset.stock;
                    precioInput.value = precio;
                    stockSpan.textContent = `Stock disponible: ${stock}`;
                } else {
                    precioInput.value = '';
                    stockSpan.textContent = '';
                }
            }

            function agregarDetalle() {
                // Validaciones
                if (!validarCamposDetalle()) return;

                const libro = libroSelect.selectedOptions[0];
                const cantidad = parseInt(cantidadInput.value);
                const precio = parseFloat(precioInput.value);
                const subtotal = cantidad * precio;

                // Crear objeto detalle
                const detalle = {
                    libroId: libro.value,
                    cantidad: cantidad,
                    precioUnitario: precio,
                    subtotal: subtotal
                };

                // Agregar a la colección
                detallesCompra.push(detalle);

                // Agregar fila a la tabla
                const fila = document.createElement('tr');
                fila.innerHTML = `
                            <td>${libro.text}</td>
                            <td class="text-center">${cantidad}</td>
                            <td class="text-end">$ ${precio.toFixed(2)}</td>
                            <td class="text-end">$ ${subtotal.toFixed(2)}</td>
                            <td class="text-center">
                                <button type="button" class="btn btn-danger btn-sm"
                                        onclick="eliminarDetalle(${contadorDetalles})">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </td>
                            <input type="hidden" name="DetallesCompra[${contadorDetalles}].LibroId" value="${libro.value}">
                            <input type="hidden" name="DetallesCompra[${contadorDetalles}].Cantidad" value="${cantidad}">
                             <input type="hidden" name="DetallesCompra[${contadorDetalles}].PrecioUnitario" value="${precio}">
                        `;

                detalleTabla.appendChild(fila);
                contadorDetalles++;

                actualizarTotal();
                limpiarCamposDetalle();
            }

            function validarCamposDetalle() {
                if (!libroSelect.value) {
                    alert('Seleccione un libro');
                    libroSelect.focus();
                    return false;
                }

                const cantidad = parseInt(cantidadInput.value);

                if (!cantidad || cantidad < 1) {
                    alert('Ingrese una cantidad válida');
                    cantidadInput.focus();
                    return false;
                }

                return true;
            }

            function limpiarCamposDetalle() {
                libroSelect.value = '';
                cantidadInput.value = '';
                precioInput.value = '';
                stockSpan.textContent = '';
            }

            function actualizarTotal() {
                const total = detallesCompra.reduce((sum, detalle) => sum + detalle.subtotal, 0);
                totalCompraSpan.textContent = total.toFixed(2);
            }

            window.eliminarDetalle = function (index) {
                detallesCompra.splice(index, 1);
                detalleTabla.removeChild(detalleTabla.children[index]);
                actualizarTotal();
            };

            function validarFormulario(e) {
                if (detallesCompra.length === 0) {
                    e.preventDefault();
                    alert('Debe agregar al menos un producto a la compra');
                    return false;
                }
                return true;
            }
        });
    </script>

    <style>
        .required::after {
            content: " *";
            color: red;
        }

        .table th {
            background-color: #f8f9fa;
        }
    </style>
}
